/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // reactCompiler: true, // Disabled - not yet fully compatible with Turbopack on Windows
};

export default nextConfig;
